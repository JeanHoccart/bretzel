"""Dedicated e2e fixture apps — minimal Bretzel apps the browser suite
drives, DECOUPLED from ``examples/`` so a demo-side refactor can never
silently break e2e again (see tests/consistency/test_fixture_apps_import).
"""
